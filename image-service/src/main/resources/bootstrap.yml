spring:
  application:
    name: image-service
  cloud:
    config:
      uri: http://host.docker.internal:8081
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://host.docker.internal:8086